events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Configuration pour le développement (HTTP)
    server {
        listen 80;
        server_name brasseriechezju.fr www.brasseriechezju.fr;

        location /.well-known/acme-challenge/ {
            root /usr/share/nginx/html;
        }

        # En développement, on redirige directement vers l'application Nuxt
        location / {
            proxy_pass http://nuxt-app:3000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }
    }

    # Configuration SSL (commentée pour le développement)
    # server {
    #     listen 443 ssl;
    #     server_name brasseriechezju.fr www.brasseriechezju.fr;
    # 
    #     ssl_certificate /etc/letsencrypt/live/brasseriechezju.fr/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/brasseriechezju.fr/privkey.pem;
    # 
    #     location / {
    #         proxy_pass http://nuxt-app:3000;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection 'upgrade';
    #         proxy_set_header Host $host;
    #         proxy_cache_bypass $http_upgrade;
    #     }
    # }
}
